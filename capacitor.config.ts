import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.efecode.TeLlevoAPP',
  appName: 'TeLlevoAPP',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
